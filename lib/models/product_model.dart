class Product {
  final String name;
  final double price;
  final String imageUrl; // Para imagens fictícias (podes usar assets ou URL)

  Product({
    required this.name,
    required this.price,
    required this.imageUrl,
  });
}